name: Develop Build (deprecated)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated workflow. Use deploy-internal.yml."
