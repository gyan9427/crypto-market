name: Rollback Release (deprecated)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated workflow. Follow manual rollback SOP in docs."
